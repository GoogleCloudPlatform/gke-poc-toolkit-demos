kind: ClusterSelector
apiVersion: configmanagement.gke.io/v1
metadata:
  name: selector-us-west1
spec:
  selector:
    matchExpressions:
      - key: location
        operator: In
        values:
        - "us-west1"