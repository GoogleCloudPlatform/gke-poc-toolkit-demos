kind: ClusterSelector
apiVersion: configmanagement.gke.io/v1
metadata:
  name: selector-wave-one
spec:
  selector:
    matchExpressions:
      - key: wave
        operator: In
        values:
        - "one"